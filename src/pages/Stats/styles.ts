import styled from 'styled-components';
import { DefaultTitle } from '../../components/Utils/Title/styles';

export const StatsTitle = styled(DefaultTitle)`
  margin-left: 1rem;

  font-size: 1.2rem;
`;
