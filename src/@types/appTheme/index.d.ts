import { DefaultTheme } from 'styled-components';

export interface AppTheme extends DefaultTheme {
  background: string;
  main: string;
  backgroundBlocks: string;
  primary: string;
  secondary: string;
  textColor: string;
  backgroundApp: string;
  backgroundInsideBlocks: string;
}
